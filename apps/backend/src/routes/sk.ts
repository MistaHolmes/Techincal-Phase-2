import { Router } from 'express';

const router = Router();

// ==========================================
// Your dedicated endpoint handlers go here!
// ==========================================

// Example route:
// router.get('/hello', (req, res) => {
//   res.json({ message: 'Hello from sk!' });
// });

export default router;
